# 📐 Визуальная структура CameraView

## Иерархия компонентов (как в Xcode)

```
CameraView.swift
│
├─ @State currentZoomLevel: Double = 1.0
│
└─ body: some View
   │
   └─ GeometryReader
      │
      └─ VStack(spacing: 0)
         │
         ├─ [1] CameraTopSafeArea
         │      └─ Color.black
         │         └─ .frame(height: 50)
         │
         ├─ [2] ZStack(alignment: .bottom)
         │      │
         │      ├─ CameraPreviewLayer
         │      │  └─ Rectangle (серый плейсхолдер)
         │      │     └─ .cornerRadius(16)
         │      │
         │      └─ ZoomControls(currentZoomLevel: $currentZoomLevel)
         │         │  └─ .padding(.bottom, 20)
         │         │
         │         └─ HStack(spacing: 20)
         │            ├─ ZoomButton(label: "0.5")
         │            ├─ ZoomButton(label: "1x") ← активная
         │            └─ ZoomButton(label: "2")
         │
         └─ [3] BottomControlPanel
                │
                └─ ZStack
                   ├─ Color.black
                   │  └─ .frame(height: 150)
                   │
                   └─ HStack
                      ├─ GalleryButton
                      ├─ Spacer()
                      ├─ ShutterButton
                      ├─ Spacer()
                      └─ FlipCameraButton
```

---

## Визуальное представление экрана

```
┌────────────────────────────────────┐
│                                    │ ← CameraTopSafeArea
│          (Dynamic Island)          │   (50px высота)
│                                    │
├────────────────────────────────────┤
│                                    │
│                                    │
│                                    │
│        CameraPreviewLayer          │
│          (Серый прямоугольник)     │
│                                    │
│                                    │
│                                    │
│                                    │
│              ┌───┬───┬───┐         │ ← ZoomControls
│              │0.5│1x │ 2 │         │   overlay поверх камеры
│              └───┴───┴───┘         │   .padding(.bottom, 20)
│                                    │
├────────────────────────────────────┤
│                                    │
│  ●                 ⊚               │ ← BottomControlPanel
│ [📷]              [⊙]        [⟲]   │   (150px высота)
│Gallery          Shutter       Flip │
│                                    │
└────────────────────────────────────┘
```

---

## Компоненты по слоям

### Слой 1: Верхний отступ
```
┌────────────────────────────────────┐
│    CameraTopSafeArea (50px)        │
│    • Color.black                   │
│    • Отступ для Dynamic Island     │
└────────────────────────────────────┘
```

### Слой 2: Камера + Зум (ZStack)
```
┌────────────────────────────────────┐
│                                    │
│   CameraPreviewLayer (фон)         │
│   ┌──────────────────────────────┐ │
│   │   Rectangle (серый)          │ │
│   │   .cornerRadius(16)          │ │
│   │                              │ │
│   │                              │ │
│   └──────────────────────────────┘ │
│                                    │
│   ZoomControls (overlay)           │
│        ┌────┬────┬────┐            │
│        │0.5 │ 1x │  2 │            │
│        └────┴────┴────┘            │
└────────────────────────────────────┘
```

### Слой 3: Нижняя панель
```
┌────────────────────────────────────┐
│   BottomControlPanel (150px)       │
│                                    │
│   GalleryButton   ShutterButton    │
│        ●               ⊙           │
│     (45x45)         (74x74)        │
│                                    │
│                    FlipCameraButton│
│                           ⟲        │
│                        (45x45)     │
└────────────────────────────────────┘
```

---

## Детализация кнопок

### ZoomButton (одна кнопка)
```
     Неактивная           Активная
    ┌────────┐           ┌────────┐
    │        │           │        │
    │  0.5   │           │  1x    │
    │ белый  │           │ жёлтый │
    │        │           │        │
    └────────┘           └────────┘
     opacity: 0.3        opacity: 0.6
     цвет: white         цвет: accentYellow
```

### ShutterButton
```
    ┌────────────┐
    │  ┌──────┐  │
    │  │      │  │  ← Внутренний круг (64px)
    │  │  ⊙   │  │     fill: white
    │  │      │  │
    │  └──────┘  │
    └────────────┘
    ↑ Внешнее кольцо (74px)
      stroke: white, lineWidth: 4
```

### GalleryButton
```
    ┌────────┐
    │        │
    │   📷   │  ← Image: "photo"
    │        │     размер: 20px
    └────────┘
    45x45, серый фон + обводка
```

### FlipCameraButton
```
    ┌────────┐
    │        │
    │   ⟲    │  ← Image: "arrow.triangle.2.circlepath"
    │        │     размер: 24px
    └────────┘
    45x45, прозрачный фон
```

---

## Граф зависимостей файлов

```
CameraView.swift
    ↓
    ├─→ ComponentsCameraTopSafeArea.swift
    │
    ├─→ ComponentsCameraPreviewLayer.swift
    │       └─→ AppTheme.swift (cornerRadius)
    │
    ├─→ ComponentsZoomControls.swift
    │       └─→ ComponentsZoomButton.swift
    │               └─→ AppTheme.swift (все константы)
    │
    └─→ ComponentsBottomControlPanel.swift
            ├─→ ComponentsGalleryButton.swift
            ├─→ ComponentsShutterButton.swift
            │       └─→ AppTheme.swift (shutterSize, colors)
            └─→ ComponentsFlipCameraButton.swift
```

---

## Поток данных (State flow)

```
CameraView
    │
    ├─ @State currentZoomLevel: Double = 1.0
    │
    └─→ ZoomControls
            │
            ├─ @Binding currentZoomLevel ← двусторонняя связь
            │
            └─→ ZoomButton (0.5x)
                    │
                    └─ action: { currentZoomLevel = 0.5 }
                         ↓
                    Обновляет State в CameraView
                         ↓
                    ZoomControls получает новое значение
                         ↓
                    Перерисовка UI (активная кнопка меняется)
```

---

## Размеры элементов (из AppTheme)

| Элемент | Константа | Значение |
|---------|-----------|----------|
| Кнопка зума | `zoomButtonSize` | 34px |
| Кнопка затвора (внешняя) | `shutterSize` | 74px |
| Кнопка затвора (внутренняя) | `shutterInnerSize` | 64px |
| Кнопка галереи | hardcoded | 45px |
| Кнопка переключения | hardcoded | 45px |
| Скругление превью | `cornerRadius` | 16px |
| Верхний отступ | hardcoded | 50px |
| Нижняя панель | hardcoded | 150px |

---

## Как читать эту схему

1. **Иерархия компонентов** - показывает вложенность SwiftUI View
2. **Визуальное представление** - как выглядит на экране
3. **Компоненты по слоям** - что на каком слое рисуется
4. **Граф зависимостей** - какие файлы от каких зависят
5. **Поток данных** - как передаётся State между компонентами

---

**Используйте эту схему для:**
- Понимания структуры проекта
- Планирования изменений
- Объяснения архитектуры команде
- Отладки проблем с layout

---

**Обновлено:** 21 января 2026
